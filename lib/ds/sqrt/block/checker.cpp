#include <bits/stdc++.h>
using namespace std;

signed main() {
	for(int i = 1; i <= 200; i++) {
		system("gen.exe"); 
		system("massChangeQuery.exe"); 
		system("modal.exe"); 
		if(system("fc out.txt modalout.txt")) {
			cout << "when the imposter is sus :blushing: \n"; 
			break; 
		}
	}
}
